import { CompareBarDirective } from './compare-bar.directive';

describe('CompareBarDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareBarDirective();
    expect(directive).toBeTruthy();
  });
});
